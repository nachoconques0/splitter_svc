/**
 * The Bill the page shows. More than one Bill in the UI is out of scope, so this
 * is a constant rather than a route parameter.
 */
export const BILL_ID = 'b1110000-0000-4000-8000-000000000001'
